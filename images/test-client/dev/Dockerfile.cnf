ARG DIR_ROOT=/app

FROM node:18 as base
ARG DIR_ROOT
WORKDIR ${DIR_ROOT}
COPY package.json ${DIR_ROOT}/package.json
RUN npm install 

FROM base as stage
ARG DIR_ROOT
COPY images/test-client/package.json ${DIR_ROOT}/images/test-client/package.json

COPY packages/@moviemasher/client-lib/package.json ${DIR_ROOT}/packages/@moviemasher/client-lib/package.json
COPY packages/@moviemasher/shared-lib/package.json ${DIR_ROOT}/packages/@moviemasher/shared-lib/package.json

RUN npm install 


FROM base 
ARG DIR_ROOT

COPY --from=stage ${DIR_ROOT}/node_modules ${DIR_ROOT}/node_modules

RUN npx playwright install --with-deps


ENTRYPOINT ["npm", "run", "--workspace=@moviemasher/test-client"]
CMD ["test"]

LABEL org.opencontainers.image.authors="support@moviemasher.com"