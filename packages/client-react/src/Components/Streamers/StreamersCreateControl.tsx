import React from "react"
import { PropsWithChildren, ReactResult } from "../../declarations"
import { View } from "../../Utilities/View"

export function StreamersCreateControl(props: PropsWithChildren): ReactResult {

  return (
    <View {...props} />
  )
}
