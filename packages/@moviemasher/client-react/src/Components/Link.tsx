import { createComponent } from '@lit/react'
import { LinkElement, LinkTag } from '@moviemasher/client-lib/component/link.js'
import React from 'react'
/** @category Components */
export const Link = createComponent({ tagName: LinkTag, elementClass: LinkElement, react: React })