import { createComponent } from '@lit/react'
import { PickerElement, PickerTag } from '@moviemasher/client-lib/component/picker.js'
import React from 'react'
/** @category Components */
export const Picker = createComponent({ tagName: PickerTag, elementClass: PickerElement, react: React })