import { createComponent } from '@lit/react'
import { TimelineIconElement, TimelineIconTag } from '@moviemasher/client-lib/timeline/timeline-icon.js'
import React from 'react'
/** @category Components */
export const TimelineIcon = createComponent({ tagName: TimelineIconTag, elementClass: TimelineIconElement, react: React })