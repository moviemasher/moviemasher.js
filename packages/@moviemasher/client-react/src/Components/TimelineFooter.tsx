import { createComponent } from '@lit/react'
import { TimelineFooterElement, TimelineFooterTag } from '@moviemasher/client-lib/timeline/timeline-footer.js'
import React from 'react'
/** @category Components */
export const TimelineFooter = createComponent({ tagName: TimelineFooterTag, elementClass: TimelineFooterElement, react: React })