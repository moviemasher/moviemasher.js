import { createComponent } from '@lit/react'
import { ButtonElement, ButtonTag } from '@moviemasher/client-lib/component/button.js'
import React from 'react'
/** @category Components */
export const Button = createComponent({ tagName: ButtonTag, elementClass: ButtonElement, react: React })