import { createComponent } from '@lit/react'
import { IconElement, IconTag } from '@moviemasher/client-lib/component/icon.js'
import React from 'react'
/** @category Components */
export const Icon = createComponent({ tagName: IconTag, elementClass: IconElement, react: React })