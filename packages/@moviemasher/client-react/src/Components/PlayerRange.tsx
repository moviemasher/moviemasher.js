import { createComponent } from '@lit/react'
import { PlayerRangeElement, PlayerRangeTag } from '@moviemasher/client-lib/player/player-range.js'
import React from 'react'
/** @category Components */
export const PlayerRange = createComponent({ tagName: PlayerRangeTag, elementClass: PlayerRangeElement, react: React })