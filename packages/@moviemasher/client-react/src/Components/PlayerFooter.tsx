import { createComponent } from '@lit/react'
import { PlayerFooterElement, PlayerFooterTag } from '@moviemasher/client-lib/player/player-footer.js'
import React from 'react'
/** @category Components */
export const PlayerFooter = createComponent({ tagName: PlayerFooterTag, elementClass: PlayerFooterElement, react: React })