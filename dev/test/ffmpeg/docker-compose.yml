version: "3"
services:
  ffmpeg:
    image: moviemasher/moviemasher.js:5.1.0
    # image: jrottenberg/ffmpeg
    container_name: ffmpeg
    volumes:
      - ../../../temporary/ffmpeg:/temp/ffmpeg
    working_dir: /temp/ffmpeg
    entrypoint: ffmpeg
    # command: -formats
    # command: -y -t 10 -r 30 -width 1280 -height 480 -keep_ar 0 -i mm.svg -s 640x480 -pix_fmt yuv420p -f:v mp4 -c:v libx264 -level 41 -movflags faststart -g 60 -t 10 -r 30 output.mp4
    command: -y  -loop 1 -s 640x480 -i mm.svg -s 640x480 -f:v mp4 -c:v libx264 -level 41 -movflags faststart -g 60 -t 10 -r 30 -b:v 4M -pix_fmt yuv420p output.mp4
    #  output.mov
